(define mac
           (lambda (n)
                   (if (> n 100)
                          (- n 10)
           
                           
                   (if(<= n 100)
                           (ack (ack (+ n 11))))
                           )))
